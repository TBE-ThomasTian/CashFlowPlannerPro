<UserControl x:Class="CashFlowPlannerPro.Views.FixkostenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:CashFlowPlannerPro.Converters">
    <UserControl.Resources>
        <conv:DateDisplayConverter x:Key="DateDisplayConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource GridLineBrush}" BorderThickness="0,0,0,1" Padding="12,10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="AddBtn" Content="&#x2795; Neu" Style="{StaticResource AddButton}" Click="Add_Click" Margin="0,0,8,0"/>
                <Button x:Name="EditBtn" Content="Bearbeiten" Style="{StaticResource NavButton}" Click="Edit_Click" Margin="0,0,8,0" Padding="10,4"/>
                <Button x:Name="DeleteBtn" Content="&#x1F5D1;&#xFE0F; L&#246;schen" Style="{StaticResource DeleteButton}" Command="{Binding DeleteCommand}"/>
            </StackPanel>
        </Border>
        <DataGrid x:Name="FixkostenGrid" Grid.Row="1" ItemsSource="{Binding Fixkosten}" SelectedItem="{Binding SelectedTransaction}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  MouseDoubleClick="FixkostenGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Datum" Binding="{Binding Date, Converter={StaticResource DateDisplayConverter}}" Width="110"/>
                <DataGridTextColumn Header="Beschreibung" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Betrag &#x20AC;" Binding="{Binding Amount, StringFormat={}{0:N2}}" Width="120"/>
                <DataGridTextColumn Header="Kategorie" Binding="{Binding CategoryName}" Width="150"/>
                <DataGridTextColumn Header="Intervall" Binding="{Binding Interval}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
